/**
 * Set config
 */

/**
 * Port Running
 */
app.set('port', process.env.PORT || 3000);

/**
 * Base path
 */
app.set('path', 	'/');

/**
 * Getup register path
 */
app.set('register', '/');

/**
 * Default language
 */
app.set('language', 'pt');

/**
 * Debug mode
 */
app.set('debug', true);
